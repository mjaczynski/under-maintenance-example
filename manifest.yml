#--------------------------------------
# change the `host` property to a
# unique hostname on the domain you
# are deploying to.
#--------------------------------------

applications:
- name:    under-maintenance
  host:    under-maintenance-mja
  memory:  128M
  command: node server.js
